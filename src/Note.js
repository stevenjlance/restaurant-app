import React from "react";

const Note = () => {
  return (
    <div className="note">
      <strong>NOTE:</strong>
      <ul>
        <li>
          "Grade Pending" means that the restaurant got either a B or a C. The
          restaurant can contest the grade, and while it's waiting for a
          hearing, it can post the grade it received or the “Grade Pending”
          sign.
        </li>
      </ul>
    </div>
  );
};

export default Note;
